version: "3.9"

services:
  api:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    working_dir: /app
    volumes:
      - ../:/app
    command: ["python", "-m", "uvicorn", "services.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
